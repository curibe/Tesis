import numpy as np
import os
import commands as cm
from Jupiter import *
from Config import *
from ICond import *

#==============
# SHORCUTS
#==============
system=os.system
get=cm.getoutput
getdir=os.getcwd
chdir=os.chdir

#==============
# PATHS
#==============
#HOME=get("echo $HOME")
Principal=getdir()
InitDir=Principal+"/Cond_Iniciales/"
BinDir=Principal+"/BIN/"
RunDir=Principal+"/RUNDIR/"

cmd="ls -d "+RunDir+"* | wc -l"
Ndir=int(get(cmd))

for i in range(1,Ndir+1):
    dir= RunDir + "run%d/"%i
    chdir(dir)
    system("make -s clean prepare")
    #path=get("pwd")
    #print "path: "+ path
    #chdir(Principal)
